import styled from 'styled-components'
import vars from '../../styles/vars'

export const HeaderContainer = styled.header`
  margin-top: 2rem;
  padding: 2rem;
  border-radius: 8px;
`

export const Title = styled.h1`
  font-size: 2rem;
  color: ${vars.fontDark};
`

export const ContatoInfo = styled.p`
  margin-top: 0.5rem;
  color: ${vars.fontNormal};
`
